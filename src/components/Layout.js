import React from "react";
import HomePage from "./HomePage";

const Layout = () => {
  return (
    <div
      style={{
        height: "100%",
      }}
    >
      <HomePage />
    </div>
  );
};

export default Layout;

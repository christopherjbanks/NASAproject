import React from "react";
import HomePage from "./HomePage";

const Layout = () => {
  return (
    <div
      style={{
        position: "relative",
        height: "100vh",
      }}
    >
      <HomePage />
    </div>
  );
};

export default Layout;
